{
  "name": "start-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "cross-env GITHUB_PAGES_BUILD=true npm run build && gh-pages -d dist",
    "build:gh": "vite build --mode gh"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "gh-pages": "^6.3.0",
    "sass": "^1.100.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}